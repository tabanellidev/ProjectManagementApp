<% if !user.assignments %>
    <hr>
    <div class="row">
        <div class="col-sm-7">
            <p class="text-muted mb-0 align-middle"> Nessun compito assegnato </p>
        </div>
    </div>
    <% else %>
    <% user.assignments.each do |assignment| %>
        <% if assignment.status == 'Uncompleted' || assignment.status == 'Expired' %>
            <hr>
            <div class="row">
                <div class="col-sm-2 ">
                    <span class="mb-0 align-middle"> <%= assignment.id %></span>
                </div>
                <div class="col-sm-5">
                    <span class="mb-0 align-middle"> <%= assignment.title %> </span>
                </div>
                <div class="col-sm-3">
                    <span class="mb-0 align-middle"> <%= assignment.status %> </span>
                </div>
                <div class="col-sm-1">
                    <%= button_to "Info", assignment_path(assignment.id), method: :get, class: "btn btn-outline-primary"%>
                </div>
            </div>
        <% end %>
    <% end %>
<% end %>